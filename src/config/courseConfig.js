export const COURSE_SLUG =
  (import.meta.env.VITE_COURSE_SLUG || "tracy-course-1").trim();

export const COURSE_TITLE =
  (import.meta.env.VITE_COURSE_TITLE || "Understanding & Healing the Immigrant Parent Dynamic").trim();

export const COURSE_PRICE_LABEL =
  (import.meta.env.VITE_COURSE_PRICE_LABEL || "$7").trim();

export const COURSE_VIMEO_EMBED_URL =
  (import.meta.env.VITE_COURSE_VIMEO_EMBED_URL || "").trim();

export const COURSE_PREVIEW_VIMEO_EMBED_URL =
  (import.meta.env.VITE_COURSE_PREVIEW_VIMEO_EMBED_URL || "").trim();
